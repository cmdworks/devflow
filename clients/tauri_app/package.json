{
  "name": "devflow-desktop",
  "version": "0.1.0",
  "description": "DevFlow Desktop Companion App",
  "private": true,
  "scripts": {
    "dev": "cargo run --manifest-path src-tauri/Cargo.toml",
    "dev:mac-mtp": "cargo run --manifest-path src-tauri/Cargo.toml -- gui ../../../mac-mtp",
    "build": "cargo build --release --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri"
  }
}
